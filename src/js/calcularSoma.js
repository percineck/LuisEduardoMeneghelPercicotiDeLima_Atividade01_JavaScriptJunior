function calcularSoma() {
    // Declaração de variáveis
    let soma = 0;
    let i = 1;
  
    // Loop para calcular a soma
    while (i <= 100) {
      soma += i;
      i++;
    }
  
    // Exibe o resultado
    window.alert("A soma é: " + soma);
  }
  